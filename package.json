{
  "name": "storybook-generator-cli",
  "version": "1.0.2",
  "description": "A CLI override that adds a storybook file when generating a component.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && jasmine src/**/*_spec.js"
  },
  "keywords": [
    "schematics"
  ],
  "author": "Hydrataur",
  "license": "MIT",
  "schematics": "./src/collection.json",
  "dependencies": {
    "@angular-devkit/core": "^13.1.3",
    "@angular-devkit/schematics": "^13.1.3",
    "@schematics/angular": "^13.1.3",
    "typescript": "~4.5.2"
  },
  "devDependencies": {
    "@types/node": "^12.11.1",
    "@types/jasmine": "~3.10.0",
    "jasmine": "^3.5.0"
  }
}
